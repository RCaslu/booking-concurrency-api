[pytest]
markers =
    unit: pure domain/unit tests, no infra
    integration: tests requiring a real Postgres via TestClient
    concurrency: tests validating race-condition safety under real concurrent load
